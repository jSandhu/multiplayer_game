namespace Common.Npcs
{
    public enum NpcRole { MeleeTank, MeleeFighter, Caster, Healer}
    public enum NpcRace { Troll, Orc, Human, Undead, Elf, Dwarf}
    public enum NpcSize { Small, Medium, Large}
}
